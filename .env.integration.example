# Copy to .env.integration and fill in values from your deployed stack (cdk deploy outputs)
# Required for: npm run test:integration

API_URL=
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
AWS_REGION=us-east-1
